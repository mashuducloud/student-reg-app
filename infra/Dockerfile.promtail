# infra/Dockerfile.promtail
# infra/Dockerfile.promtail
FROM grafana/promtail:2.9.8

# No COPY: config is provided via volume in docker-compose
# docker-compose.yml mounts:
#   ./promtail-config.yaml:/etc/promtail/config.yaml:ro

# Optional default command (also set in compose)
CMD ["-config.file=/etc/promtail/config.yaml"]
